spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/server
    username: $USERNAME
    password: $PASSWORD
  jpa:
    hibernate:
      ddl-auto: update
